@model IList<ChitalkaMVC.Storage.Entities.Author>
 
@{Layout = null;}

<div class="list-group list-group-flush">
    @foreach(var author in Model)
    {
        <button type="button" class="list-group-item list-group-item-action">@author.Name @author.Country?.Name</button>
    }
</div>