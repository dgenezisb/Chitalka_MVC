@model IList<ChitalkaMVC.Storage.Entities.Country>
 
@{Layout = null;}

<div class="list-group list-group-flush">
    @foreach(var country in Model)
    {
        <button type="button" class="list-group-item list-group-item-action">@country.Name</button>
    }
</div>